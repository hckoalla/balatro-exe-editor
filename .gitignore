# Proprietário / local — nunca versionar. Barra inicial: só a raiz, NÃO
# test/fixtures/game.lua (esse é sintético e precisa ser versionado).
/game.lua
*.exe
fixtures/real/

# Assets reais do jogo (extraídos do balatro.exe pra referência/prototipagem — fontes, sons,
# texturas, shaders). Mesmo motivo do /game.lua: conteúdo proprietário, nunca versionar.
/fonts/
/shaders/
/sounds/
/textures/

# Código-fonte COMPLETO do jogo, extraído do balatro.exe pra investigação de backlog (ver
# bee10-jokers-iniciais, 19/ago/26). Ainda mais sensível que os itens acima — nunca versionar.
/balatro-exe-source-code/

# Node
node_modules/
dist/
dist-electron/
release/
.tsc-out/
*.tsbuildinfo

# Vite
.vite/

# Env
.env
.env.local

# Editor / OS
.DS_Store
Thumbs.db
